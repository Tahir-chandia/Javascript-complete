// Truthy/Falsy Values

// Falsy values means false values and these are falsy values

// "" , 0 , -0 , 0n , undefined , null , NaN ,false

// Truthy values means true values all values except falsy values are truthy values

let name = "" ? "Tahir": "Empty"
let name2 = 0 ? "Tahir": "Empty"
let name3 = 2 ? "Tahir": "Empty"
console.log(name);
console.log(name2);
console.log(name3);

