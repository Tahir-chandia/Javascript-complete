// comments

// there are two type of comments number 1 single line

/* number 2
multiple line */

// there are three ways to make variable by let,var and const

// there are three steps of variables

// 1)Declaration
// 2)Initialisation
// 3)Use

// 1)Declaration

let a; // Declaration

// 2)Initialisation

a = 2; //Initialisation

// 3)Use

console.log(a); //Use of variable

//Multiple declaration at once

let b, c;

//Multiple initialisation at once

(b = "bhai"), (c = "dost");

// Mulitiple usage of varibles at once

console.log(b, c);

// Only declaration and use not initialised

let d;
console.log(d);
console.log((d = "don")); //Initialise + use value
d = "dada"; //Initialise value
console.log(d);

// Multiple declaration + initialisation in single line

let ab = 25,
  bc = 33,
  cd = "Khan";
console.log(ab, bc, cd); //multiple variable uses at once

// Multiple declaration + initialisation in multiple lines line
let as;
let ad;
let de;

as = "asad";
ad = "ahad";
de = "devil";

console.log(as);
console.log(ad);
console.log(de);
console.log(as, ad, de);
