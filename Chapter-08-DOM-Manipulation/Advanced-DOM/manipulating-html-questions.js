// 1. Create a paragraph with text “mera naam tahir h” and add background color to black and font color to green using javascript.
/*
let createPara = document.createElement("p")
let select = document.querySelector("div")
createPara.innerHTML="“mera naam tahir h”"
select.after(createPara)
// create.style.backgroundColor ="black"
// create.style.color ="green" 
createPara.setAttribute("style",`background-color:black; color:green;`)
// 2. Insert a button with text “click me” as the first element inside the paragraph created in 1st question.
let createButton = document.createElement("button") 
createButton.innerText ="click me"
createPara.prepend(createButton)
*/

// 3. Create a <div> tag in html and give it a class & some styling. Now create a new class in css and try to append this class to the <div> element.

// let getElement= document.querySelector("div")
// let getClass = getElement.getAttribute("class")
// let class2= getElement.setAttribute("class",`${getClass} my_div`).
// console.log(getElement.nodeValue);
