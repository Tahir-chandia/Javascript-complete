// Indexing in array are same work as string

let arr = ["Karachi",24,"wild"]
console.log(arr[2]);

//typeof Array is an "Object". aray is a special type of object

console.log(typeof arr);

// Arrays are mutable . It means we can change in orignal array

arr[2] = 31
console.log(arr);
